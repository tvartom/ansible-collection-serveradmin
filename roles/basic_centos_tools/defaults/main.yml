---
# defaults file for ansible-role-basic_centos_tools
basic_default:
  timezone: "Europe/Stockholm"
  system_locale: 'sv_SE.UTF-8'
  system_language: 'en_US.UTF-8'
  package_to_install:
    - byobu
    - glibc-langpack-en
    - glibc-langpack-sv
    - perl-Image-ExifTool
    - joe #texteditor Anders använder
    - unzip
    - bzip2
    - pv

    #- net-tools #ifconfig finns i denna
    #- rsync
