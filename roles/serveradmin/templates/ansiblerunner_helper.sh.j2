#!/bin/bash
#
# Managed by Ansible!
#

# This must be ran as serveradmin!

# Exit on error:
set -e
source include_common.sh

cd {{ serveradmin.path }}/repos/serveradmin
ansible-playbook --extra-vars "target='{{ target }}' connection_type='local' ansible_started_by_user='$1'" "$2"
