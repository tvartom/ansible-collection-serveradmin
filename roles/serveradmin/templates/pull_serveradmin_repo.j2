#!/bin/bash
#
# Managed by Ansible!
#

# Exit on error:
set -e
source include_common.sh

cd {{ serveradmin.path }}/workspace/serveradmin

sudo -u {{ serveradmin.process_user }} git pull --recurse-submodules
