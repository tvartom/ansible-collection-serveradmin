#
# Generated by Ansible
#
# Ändringar i denna fil ska göras via Ansible, annars skrivs de över. / Lasse
#

HOME=/tmp
{% set counter = {'i': 0} %}
{% for application in applications_info %}
{%   for name, webinstance in application.webinstances.items() if webinstance.enable_https and webinstance.cert_handler == 'letsencrypt' %}
{{     webinstance.fullname | comment }}
{%     for item in webinstance.domaininstance_groups.values() -%}
{%       set i = counter.i %}
{#       minute: #}{{ i % 60 }} {# hour: #}{{ (5 + (i // 60) ) % 24 }} {# day: #}5 * * root	{{ application.path }}/letsencrypt/letsencrypt-renew-{{ item.fullname }}.sh >> {{ application.path }}/log/letsencrypt/{{ item.fullname }}.log 2>&1
{{       counter.__setitem__('i', i+2) }}
{%     endfor %}
{%   endfor %}
{% endfor %}

#* * * * *
#| | | | |
#| | | | +---- Day of the Week   (range: 1-7, 1 standing for Monday)
#| | | +------ Month of the Year (range: 1-12)
#| | +-------- Day of the Month  (range: 1-31)
#| +---------- Hour              (range: 0-23)
#+------------ Minute            (range: 0-59)
